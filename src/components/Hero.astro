<header class="site-header section-shell">
  <a class="brand" href="/" aria-label="Baby Sleep White Noise home">
    <svg viewBox="0 0 32 32" aria-hidden="true">
      <path d="M23.7 20.9A10 10 0 0 1 11.1 8.3 10 10 0 1 0 23.7 21Z" />
      <circle cx="22.5" cy="8.5" r="1.3" />
    </svg>
    <span>
      Baby Sleep
      <br />
      White Noise
    </span>
  </a>
  <a class="site-header__link" href="#how-to-use">
    Use gently
  </a>
</header>

<section class="hero section-shell" aria-labelledby="hero-title">
  <div class="hero__player">
    <slot />
  </div>
  <div class="hero__copy">
    <p class="eyebrow">A calmer room, one tap away</p>
    <h1 id="hero-title">
      Soft sound for <em>sleepy nights.</em>
    </h1>
    <p class="hero__intro">
      Continuous white noise to soften household sounds and settle the room. No
      sign-up. No distractions.
    </p>
  </div>
</section>

<style>
  .site-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-block: max(1.25rem, env(safe-area-inset-top)) 1.25rem;
  }
  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    color: var(--moon);
    font-size: 0.72rem;
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: 0.04em;
    text-decoration: none;
    text-transform: uppercase;
  }
  .brand svg {
    width: 2rem;
    fill: var(--warm);
  }
  .site-header__link {
    color: var(--mist);
    font-size: 0.82rem;
    text-underline-offset: 0.3rem;
  }
  .hero {
    display: grid;
    min-height: min(47rem, calc(100svh - 5rem));
    padding-block: clamp(3rem, 8vw, 6rem) clamp(5rem, 10vw, 8rem);
  }
  .hero__copy {
    position: relative;
    z-index: 1;
    max-width: 36rem;
  }
  h1 {
    margin: 0;
    font-family:
      Iowan Old Style,
      Baskerville,
      "Times New Roman",
      serif;
    font-size: clamp(3.4rem, 14vw, 6.8rem);
    font-weight: 400;
    line-height: 0.9;
    letter-spacing: -0.055em;
  }
  h1 em {
    display: block;
    color: var(--glow);
    font-weight: 400;
  }
  .hero__intro {
    max-width: 30rem;
    margin: 1.75rem 0 0;
    color: var(--mist);
    font-size: clamp(1rem, 2vw, 1.15rem);
  }
  .hero__player {
    display: grid;
    place-items: center;
    margin-bottom: 3.5rem;
  }
  @media (min-width: 800px) {
    .hero {
      grid-template-columns: minmax(24rem, 1.1fr) minmax(22rem, 0.9fr);
      align-items: center;
      gap: 2rem;
    }
    .hero__copy {
      order: -1;
    }
    .hero__player {
      margin-bottom: 0;
    }
  }
</style>
